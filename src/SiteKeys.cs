namespace Cake.Website
{
    public static class SiteKeys
    {
        // Settings
        public const string ReleaseDir = nameof(ReleaseDir); // relative to the root
        public const string CakeSourceDir = nameof(CakeSourceDir);
        public const string CakeContribSourceDir = nameof(CakeContribSourceDir);
    }
}
